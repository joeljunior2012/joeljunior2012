# Carrega variáveis de ambiente a partir de arquivo .env
python-dotenv==1.1.1

# Validação tipada das configurações da aplicação
pydantic==2.11.4
pydantic-settings==2.9.1

# Cliente HTTP para buscar dados de mercado
requests==2.32.3
